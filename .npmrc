# Traiter les dépendances optionnelles comme vraiment optionnelles
# Évite que npm install échoue si canvas / @discordjs/opus ne compilent pas
optional=true
legacy-peer-deps=true

# Ne pas traiter les erreurs de scripts post-install comme fatales
# (nécessaire pour les packages natifs comme canvas)
ignore-scripts=false
